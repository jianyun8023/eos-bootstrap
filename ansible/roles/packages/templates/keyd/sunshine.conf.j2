# {{ ansible_managed }}
# Keep Sunshine's protocol modifiers intact; remap after virtual input delivery.
[ids]
# The full ID excludes Sunshine's mice/touch/pen, which also advertise keys.
k:beef:dead:fe1a1126

[main]
leftmeta = layer(alt)
rightmeta = layer(alt)
leftalt = layer(meta)
rightalt = layer(meta)

{% include 'keyd/alt-bindings.conf.j2' %}
